FROM node:20-alpine

WORKDIR /app

# Copy package.json first to leverage Docker layer caching
COPY package.json package-lock.json* ./

RUN npm install

# Copy the rest of the files
COPY . .

# Expose Vite's default dev port
EXPOSE 5173

# Run Vite in host mode
CMD ["npm", "run", "dev"]
